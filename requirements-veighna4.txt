# VeighNa/vn.py 4.x migration environment.
# Use Python 3.10+; Python 3.13 is recommended by the upstream 4.4.0 README.

vnpy==4.4.0

# Common apps/gateways split out of the core package in 4.x.
vnpy_ctastrategy==1.4.1
vnpy_ctabacktester==1.3.0
vnpy_sqlite==1.1.3
vnpy_rqdata==3.4.7.8

# Cryptocurrency gateways/API kept for compatibility with the old in-tree
# vnpy.gateway.* and vnpy.api.* imports.
pytz>=2024.1
websocket-client>=1.8.0
vnpy_bybit==2026.6.9
vnpy_deribit==2.0.1.1
vnpy_okex==2021.4.21.5

# CTP is optional here because macOS/Apple Silicon often needs a local source
# build with a working compiler toolchain instead of a ready-made wheel.
# Install it separately when the gateway is required:
#   python -m pip install vnpy_ctp==6.7.11.4
# or build from https://github.com/vnpy/vnpy_ctp
# vnpy_ctp==6.7.11.4
